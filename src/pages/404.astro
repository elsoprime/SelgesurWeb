---
import bgServices from '../assets/images/bg-services.jpeg'
import Layout from '../layouts/Layout.astro'
---

<Layout
  title='Página no encontrada'
  subtitle='Lo sentimos, la página que buscas no existe'
  bgImage={bgServices.src}
  isHome={true}
>
  <!-- Contenedor padre que ocupa toda la altura -->
  <div class='min-h-screen w-full flex items-center justify-center px-4 py-8'>
    <!-- Content -->
    <div class='relative z-20 w-full max-w-4xl text-center'>
      <div
        class='bg-white/95 backdrop-blur-sm rounded-3xl p-8 md:p-16 shadow-2xl border border-white/80'
      >
        <!-- 404 Number -->
        <div
          class='text-8xl md:text-[10rem] font-extrabold text-gray-800 mb-6 animate-pulse'
        >
          404
        </div>
        <!-- Message -->
        <h1 class='text-2xl md:text-4xl font-semibold text-gray-700 mb-4'>
          Página no encontrada
        </h1>
        <p class='text-gray-600 mb-8 max-w-md mx-auto leading-relaxed'>
          Lo sentimos, la página que buscas no existe. Puedes volver al inicio o
          explorar otros contenidos.
        </p>
        <!-- Home Button -->
        <a
          href='/'
          class='inline-block bg-red-600 text-gray-500 px-8 py-4 rounded-full font-semibold hover:bg-red-700 transition-all duration-300 shadow-lg hover:shadow-xl'
        >
          Volver al Inicio
        </a>
      </div>
    </div>
  </div>
</Layout>
