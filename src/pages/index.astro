---
import {Image} from 'astro:assets'
import Layout from '@/layouts/Layout.astro'
import SectionAbout from '@/components/Shared/SectionAbout.astro'
import SectionServices from '@/components/Shared/SectionServices.astro'
import SectionClients from '@/components/Shared/SectionClients.astro'
import SectionTestimonials from '@/components/Shared/SectionTestimonials.astro'
---

<Layout
  title='Inicio'
  subtitle='Expertos en operación, mantenimiento y prevención para instalaciones industriales críticas'
  useSlideshow={true}
  isHome={true}
>
  <!-- Sección Bienvenida -->
  <SectionAbout />
  <!-- Sección Servicios -->
  <SectionServices />
  <!-- Sección Clientes -->
  <SectionClients />
  <!-- Sección Testimonios -->
  <SectionTestimonials />
</Layout>
